spec_version: 1
kind: blueprint

metadata:
  description: AzureDevOps Application

clouds:
    - azure 4: eastus

applications:
  - windows-vm:   

debugging:
  availability: enabled-on

ingress:
  enabled: false
